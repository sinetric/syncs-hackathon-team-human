# Copy to frontend/.env — never commit .env itself.

# Backend origin (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000

# "true" renders the whole UI from local fixtures with no backend running —
# the venue-wifi-died switch.
VITE_USE_FIXTURES=false
